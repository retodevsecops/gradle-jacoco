name: SonarCloud analysis
on:
  workflow_dispatch:
    inputs:
      env:
        description: 'The environment to deploy for'
        default: 'dev'
        required: true
        type: string
jobs:
  workflow:
    uses: InnovacionTecnologicaCSP/devsecops-actions/.github/workflows/main.yml@main
    with:
      env: ${{ inputs.env }}
    secrets: inherit